<div class="max-w-2xl mx-auto">
  <div class="mb-6">
    <.link 
      href={~p"/admin/users"} 
      class="btn btn-sm btn-outline border-base-content"
    >
      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
      </svg>
      Back to Users
    </.link>
  </div>

  <div class="card bg-base-200 shadow-xl">
    <div class="card-body">
      <h2 class="card-title">
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path>
        </svg>
        Edit User
      </h2>

      <.form 
        for={@changeset} 
        action={~p"/admin/users/#{@user.id}"} 
        method="put"
        class="space-y-4"
      >
        <div class="form-control">
          <label class="label">
            <span class="label-text">Name <span class="text-error">*</span></span>
          </label>
          <input
            id="user_name"
            name="user[name]"
            type="text"
            value={@user.name}
            required
            class="input input-bordered w-full"
            placeholder="Enter user name"
          />
          <p :if={@changeset.errors[:name]} class="text-error text-sm mt-1">
            <%= elem(@changeset.errors[:name], 0) %>
          </p>
        </div>

        <div class="form-control">
          <label class="label">
            <span class="label-text">Email <span class="text-error">*</span></span>
          </label>
          <input
            id="user_email"
            name="user[email]"
            type="email"
            value={@user.email}
            required
            class="input input-bordered w-full"
            placeholder="Enter email address"
          />
          <p :if={@changeset.errors[:email]} class="text-error text-sm mt-1">
            <%= elem(@changeset.errors[:email], 0) %>
          </p>
        </div>

        <div class="form-control">
          <label class="label">
            <span class="label-text">Password</span>
            <span class="label-text-alt">Leave blank to keep current</span>
          </label>
          <input
            id="user_password"
            name="user[password]"
            type="password"
            class="input input-bordered w-full"
            placeholder="Enter new password"
          />
          <p :if={@changeset.errors[:password]} class="text-error text-sm mt-1">
            <%= elem(@changeset.errors[:password], 0) %>
          </p>
        </div>

        <div class="form-control">
          <label class="label">
            <span class="label-text">Department</span>
          </label>
          <select
            id="user_department_id"
            name="user[department_id]"
            class="select select-bordered w-full"
          >
            <option value="">Select Department</option>
            <option :for={dept <- @departments} value={dept.id} selected={dept.id == @user.department_id}>
              <%= dept.name %>
            </option>
          </select>
          <p :if={@changeset.errors[:department_id]} class="text-error text-sm mt-1">
            <%= elem(@changeset.errors[:department_id], 0) %>
          </p>
        </div>

        <div class="form-control">
          <label class="label">
            <span class="label-text">Role</span>
          </label>
          <select
            id="user_role_id"
            name="user[role_id]"
            class="select select-bordered w-full"
          >
            <option value="">Select Role</option>
            <option :for={role <- @roles} value={role.id} selected={role.id == @user.role_id}>
              <%= role.name %>
            </option>
          </select>
          <p :if={@changeset.errors[:role_id]} class="text-error text-sm mt-1">
            <%= elem(@changeset.errors[:role_id], 0) %>
          </p>
        </div>

        <div class="form-control">
          <label class="label cursor-pointer">
            <span class="label-text">Active</span>
            <input
              type="hidden"
              name="user[is_active]"
              value="false"
            />
            <input
              id="user_is_active"
              name="user[is_active]"
              type="checkbox"
              value="true"
              checked={@user.is_active}
              class="toggle toggle-primary"
            />
          </label>
          <p :if={@changeset.errors[:is_active]} class="text-error text-sm mt-1">
            <%= elem(@changeset.errors[:is_active], 0) %>
          </p>
        </div>

        <div class="card-actions justify-end">
          <.link 
            href={~p"/admin/users"} 
            class="btn btn-sm btn-outline border-base-content"
          >
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
            </svg>
            Cancel
          </.link>
          <button type="submit" class="btn btn-sm btn-primary">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
            </svg>
            Update User
          </button>
        </div>
      </.form>
    </div>
  </div>
</div>